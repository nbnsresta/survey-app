import { styled } from "../theme";

const Subtext = styled("p", {
  fontFamily: "Roboto",
  fontWeight: 400,
  fontSize: "$md",
  lineHeight: "2.8rem",
  color: "$midnight",
});

export default Subtext;
