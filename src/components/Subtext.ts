import { styled } from "../theme";

const Subtext = styled("p", {
  fontFamily: "Roboto",
  fontWeight: 400,
  fontSize: "$md",
  lineHeight: "28px",
  color: "$midnight",
});

export default Subtext;
