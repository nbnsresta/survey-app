import { styled } from "../theme";

const Heading2 = styled("h2", {
  fontWeight: 600,
  margin: 0,
  fontSize: "$lg",
  lineHeight: "6.4rem",
  textAlign: "center",
  color: "$midnight",
});

export default Heading2;
